[build]
  command = "composer install && npm run build"
  publish = "build_production"

# Skip builds on the develop branch to save build credits.
# Pushes to develop will NOT trigger a deploy.
# Only merges/pushes to the production branch (main) will trigger builds.
[context.develop]
  ignore = "exit 0"
