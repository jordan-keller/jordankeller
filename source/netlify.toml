[build]
  command = "npm run build"
  publish = "build_production"

[context.develop]
  command = "npm run build"
  publish = "build_local"

[context.production]
  command = "npm run build"
  publish = "build_production"