cat > netlify.toml << 'EOF'
[build]
  command = "npm run build"
  publish = "build_local"
  
[build.environment]
  PHP_VERSION = "8.2"

[context.develop]
  command = "composer install --no-dev --prefer-dist && npm run build && vendor/bin/jigsaw build local"
  publish = "build_local"

[context.branch-deploy]
  command = "composer install --no-dev --prefer-dist && npm run build && vendor/bin/jigsaw build local"
  publish = "build_local"
EOF